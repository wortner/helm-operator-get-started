---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: podinfo-dev
  namespace: dev
  annotations:
    fluxcd.io/automated: "true"
    filter.fluxcd.io/chart-image: glob:dev-*
spec:
  releaseName: podinfo-dev
  chart:
    git: git@github.com:wortner/helm-operator-get-started.git
    path: charts/podinfo
    ref: master
  values:
    image:
      repository: 192.168.10.103:5000/podinfo
      tag: niwdveki
    replicaCount: 1
    hpa:
      enabled: false
